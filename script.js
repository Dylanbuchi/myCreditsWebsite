var fud = prompt("Do you agree that Fuders need to go to Hell ?");
var why = prompt("WHY??");

var response = {
    yes: true,
    no: false
};

function fuder(yes, no) {
    if ((response === yes) {
        alert("YES")
    } else {
        alert("Wrong username or password")
    }
}

function fuder(fud, why);